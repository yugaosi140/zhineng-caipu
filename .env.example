# 复制成 .env.local（本地）或在托管平台的环境变量里配置。
# 全部可选 —— 一个都不配也能跑，只是菜价会落到 baseline 兜底价。

# 菜价接口的同源前缀。
#
# 开发环境留空即可：vite.config.js 里的 server.proxy 已经把 /pfsc 转到
# https://pfsc.agri.cn，`npm run dev` 开箱就能拿到指数和简报。
#
# 生产环境（npm run build 的静态产物）没有那层 dev proxy，必须自己在前面放
# 一层反向代理，然后把这个变量指向它。配置样例见 deploy/README.md。
#
# 例：反向代理和站点同域 → 留空或写 /pfsc
# 例：代理在独立域名上   → https://pfsc-proxy.example.com
VITE_PFSC_BASE=

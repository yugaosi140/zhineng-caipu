# 安全策略

## 支持的版本

本项目为纯前端应用，只维护 `main` 分支的最新代码。安全修复只在 `main` 上进行，不向历史版本回溯。

| 版本 | 是否支持 |
| --- | --- |
| `main` | 是 |
| 其他 tag 或分支 | 否 |

## 报告漏洞

**请不要通过公开 issue 报告安全问题。**

请走 GitHub 私密报告渠道：[新建安全公告](https://github.com/yugaosi140/zhineng-caipu/security/advisories/new)。

报告时请尽量包含：

- 问题类型与影响范围
- 相关文件路径与代码位置
- 复现步骤，或可复现的最小示例
- 你认为可行的修复方向（如果有）

维护者会尽快确认收到并给出评估结论。修复发布后会在安全公告中致谢报告者，除非你希望匿名。

## 本项目的安全边界

这些信息有助于判断某个问题是否属于本项目的安全范围：

- **没有后端、没有账号、没有数据库。** 应用完全在浏览器里运行，所有用户数据（地区偏好、手工菜价）只存在 `localStorage`，不上传任何服务器。
- **不需要 API key。** 天气数据源 Open-Meteo 免注册免密钥，代码中不存在也不应存在任何凭据。如果你在仓库里发现了疑似密钥的内容，那本身就是需要报告的问题。
- **两个外部数据源**：`api.open-meteo.com`（天气）和 `pfsc.agri.cn`（菜价指数，经开发服务器代理）。相关请求逻辑集中在 `src/api/`。
- **`vite.config.js` 中的 `/pfsc` 代理仅用于本地开发。** 生产部署需要自行提供等价代理，代理配置不当（如开放为任意 URL 转发）属于部署方的责任，但欢迎报告我们文档中的误导性建议。

以下情况通常不视为本项目的安全漏洞，但仍欢迎作为普通 issue 提出：

- 上游数据源（Open-Meteo、pfsc.agri.cn）自身的问题
- 依赖包中不影响本项目实际代码路径的告警
- 菜谱数据不准确 —— 这属于数据质量问题，请走 `菜谱与数据修正` issue 模板
